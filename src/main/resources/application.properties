# --- Server Configuration ---
# Uses the SERVER_PORT env var from AWS, or defaults to 5000 for local
server.port=${SERVER_PORT:5000}
server.shutdown=immediate
server.servlet.session.timeout=30m

# --- Application Name ---
spring.application.name=Student System Server

# --- Database Configuration ---
# Uses AWS Env Vars first. Defaults are set to your RDS instance for local testing.
# FIXED: The URL is now complete (ending in .com:3306/student_system_db)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://student-system-db.cfi8mm4iog6d.us-west-2.rds.amazonaws.com:3306/student_system_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Checkpoint_BNMIT*26}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- Spring Data JPA / Hibernate ---
# "update" automatically creates/updates tables to match your Java code
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# --- JWT Configuration ---
# Uses env var first. The default here is the key you generated earlier.
jwt.secret-key=${JWT_SECRET_KEY:4v8y/B?E(H+MbQeThWmZq3t6w9z$C&F)J@NcRfUjXn2r5u7x!A%D*G-KaPdSgVkY}

# --- Logging ---
logging.level.root=INFO
logging.level.com.studentsystem=DEBUG
logging.level.org.springframework.security=INFO